export interface IStats {
    hp?: number,
    speed?: number,
    accelerationTime?: number,
    warpSpeed?: number,
    dpmShip?: number,
    dpmAntiAir?: number,
    dpmSiege?: number,
    incomplete?: boolean;
}
